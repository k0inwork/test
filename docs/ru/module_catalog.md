# Каталог внешних модулей

Этот документ содержит подробную информацию об API внешних систем, с которыми взаимодействует система PUM через паттерн `Module.getModule`. Эти модули представляют собой специализированные контроллеры и внешние системы, разработанные различными инженерными группами.

## 1. Управление сетью и инфраструктурой

### 1.1. `ip-module` (Контроллер DNS/DHCP)
- **Реализация:** `DNSDHCPModule` в `modules/dnsdhcp.py`.
- **Предполагаемое назначение:** Управление службами сетевой идентификации (распределение IP, записи DNS).
- **Поверхность API:**
  - `load(command)`: Получает текущую конфигурацию или состояние.
  - `send(data)`: Отправляет новые сетевые конфигурации.
- **Формат данных:** Обменивается данными преимущественно в формате **YAML**, обернутом в сообщения RabbitMQ.
- **Контекст взаимодействия:** Вызывается приложением `network` для предоставления или обновления сетевых настроек управляемых устройств.

### 1.2. `switch-module` (Контроллер конфигурации коммутаторов)
- **Реализация:** `SwitchConfigModule` в `modules/equipment.py`.
- **Предполагаемое назначение:** Глубокая конфигурация и мониторинг состояния сетевых коммутаторов.
- **Поверхность API:** `status`, `get_from_device`, `add_new_switch`, `add_new_config`, `set`, `check`.
- **Динамическое отображение:** Использует `__getattr__` для сопоставления вызовов методов Python с командами RabbitMQ, разделенными пробелами (например, `add_new_switch` становится `"add new switch"`).
- **Контекст взаимодействия:** Используется для автоматизированного предоставления коммутаторов и проверки согласованности конфигурации во всей сетевой фабрике.

### 1.3. `route-module` (Контроллер маршрутизации и сессий)
- **Реализация:** `RouteModule` в `modules/route.py`.
- **Предполагаемое назначение:** Организует сессии L2/L3 и выбор пути через фабрику шлюзов.
- **Поверхность API:**
  - `create(port, bandwidth, priority, uid, session)`: Устанавливает новое соединение.
  - `remove(port, id, session)`: Разрывает существующее соединение.
- **Контекст взаимодействия:** Интенсивно используется `services/tasks.py` во время установления и разрыва сессий. Возвращает ответы JSON, содержащие установленные IP-адреса и идентификаторы соединений.

## 2. Управление оборудованием

### 2.1. `pdu-module` (Контроллер устройств распределения питания)
- **Реализация:** `PDUModule` в `modules/equipment.py`.
- **Предполагаемое назначение:** Управляет физическим электропитанием стоек и отдельных устройств.
- **Поверхность API:**
  - `statusRelayAll(ip)`: Получает состояние питания всех розеток на PDU.
  - `inverseRelay(ip, number, interval)`: Переключает состояние конкретной розетки (перезагрузка).
- **Безопасность:** Требует жестко закодированные учетные данные (`PDU_DEVICE_USER/PASSWORD`), передаваемые в каждом запросе.

### 2.2. `ipmi-module` (Контроллер внеполосного управления серверами)
- **Реализация:** `IPMIModule` в `modules/equipment.py`.
- **Предполагаемое назначение:** Удаленное управление серверами (взаимодействие с BMC — Baseboard Management Controller).
- **Поверхность API:** `status`, `power_on`, `soft_power_off`, `hard_reset`, `ipmi_cold_reset`.
- **Контекст взаимодействия:** Обеспечивает управление «последней надежды» для не отвечающих серверов, позволяя системе выполнять жесткую перезагрузку или контролировать состояние оборудования независимо от ОС.

## 3. Интеграция с внешними системами

### 3.1. `asux-module` (Интеграция с ASU-X)
- **Реализация:** `ASUXModule` в `modules/asu_iks.py`.
- **Предполагаемое назначение:** Интеграция с системой мониторинга/управления более высокого уровня или параллельной системой (ASU-X).
- **Поверхность API:** `request(skip_wait=True)`.
- **Контекст взаимодействия:** Используется для синхронизации состояния или «включения» отслеживания во внешней системе. Поддерживает асинхронные запросы, когда система не ждет немедленного ответа.

### 3.2. `ready-module` (Фактор здоровья и готовности)
- **Реализация:** `ReadyModule` в `modules/ready.py`.
- **Предполагаемое назначение:** Агрегирует метрики здоровья в единый фактор «готовности» для управляемой среды.
- **Поверхность API:** `getdata()`.
- **Контекст взаимодействия:** Вызывается представлениями дашборда для отображения общего показателя «здоровья» всего управляемого объекта.

## 4. Сводная таблица взаимодействия

| Модуль | Протокол | Формат | Типичный потребитель |
|--------|----------|--------|------------------|
| `ip-module` | RabbitMQ | YAML | `network/actions.py` |
| `switch-module` | RabbitMQ | JSON | `data/tasks.py` |
| `route-module` | RabbitMQ | JSON | `services/tasks.py` |
| `pdu-module` | RabbitMQ | JSON | `data/actions.py` |
| `ipmi-module` | RabbitMQ | JSON | `data/tasks.py` |
| `asux-module` | RabbitMQ | JSON | `core/views.py` |
| `ready-module` | RabbitMQ | JSON | `core/views.py` |
