There are rules
1) You have a GH_TOKEN to access github
2) All heavy lifting with docker should be done in github workflow, you can access docker
3) push aggressively, as changed would be lost if something happens and vm sleeps
